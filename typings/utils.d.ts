/** ========================================================================= *\
 * @module Utilities
 * ========================================================================== */
/** findInArray returns the value of the first element that satisfies the predicate. */
export declare function findInArray<T>(arr: T[], predicate: (val: T) => boolean): T | undefined;
